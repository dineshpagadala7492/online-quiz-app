<!DOCTYPE html>
<html>
<head>
    <title>Quiz</title>
</head>
<body>
    <h1>Quiz</h1>
    <form method="POST">
        {% for i, q in enumerate(questions) %}
            <p>{{ q.q }}</p>
            {% for option in q.options %}
                <input type="radio" name="q{{ i }}" value="{{ option }}" required> {{ option }}<br>
            {% endfor %}
        {% endfor %}
        <button type="submit">Submit</button>
    </form>
</body>
</html>
